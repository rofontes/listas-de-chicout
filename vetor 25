#include <stdio.h>
#include <conio.h>
main()
<code>{
    int i, tam[100], q=0;
    
    for(i=0;i<100;i=i+1)
    {    
        printf("\n%d : ", i);
        if(tam%7 !=0);        
    }
    printf("\n%i numeros nao sao multiplos de  sete:",q);
    getchar();
}
